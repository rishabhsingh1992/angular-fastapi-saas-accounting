import { Injectable } from '@angular/core';

@Injectable({
    providedIn: 'root',
})
export class AuthService {
    signIn(): void {
        // Placeholder for API-backed auth flow.
    }

    register(): void {
        // Placeholder for API-backed registration flow.
    }
}
